[project]
name = "kcs-harness"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "baesang", email = "baesang0522@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "langchain>=1.4.0",
    "langgraph>=1.2.11",
]

[project.scripts]
kcs-harness = "kcs_harness:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
