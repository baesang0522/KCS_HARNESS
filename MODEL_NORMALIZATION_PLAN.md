# Excel 업무 지원 Harness — 핵심 계획

> 2026-09-16 · 현재 코드 기준 초안. 파일명은 유지하되 모델규격 외 Excel 작업도 포함한다.
> 실제 프로젝트: `/Users/bae/Desktop/workspace/vibe_coding/KCS_HARNESS`

## 1. 목적과 기본 원칙

Excel 오른쪽 Add-in에서 LLM의 도움을 받아 정제·수식 등 Excel 업무를 수행한다. 선택한 원본뿐 아니라 사용자가 가져온 기업 ERP 데이터, 정답을 붙인 예시, 별도 참조표도 함께 확인해 처리 방향을 제안한다. 숙련 직원의 전처리 방식과 적용 경험을 미리 저장하고, 현재 데이터와 업무에 맞는 방식을 추천하는 데 활용한다. 추가 작업 종류는 실제 요구에 따라 확장한다.

- **추가 자료 활용**: 사용자가 원본·참조자료·정답 예시의 역할과 대응 열을 지정한다. 자료의 출처와 매칭 근거를 보존하고, 값이 없거나 서로 충돌하면 확인을 요청한다. 외부 자료도 무조건 정답으로 간주하지 않는다.
- **숙련자 지식 활용**: 전처리 규칙과 함께 적용 조건·예외·전후 예시·제안 이유를 저장한다. 현재 작업에 맞는 방법을 추천하되 최종 적용은 사용자 승인을 따른다.
- LLM은 데이터를 해석하고 처리 방향·규칙·수식을 제안한다. 검증된 Python 함수가 정제·집계를 수행하고 Office.js가 Excel을 읽고 쓴다.
- 기본 흐름: **요청 → 범위·열 확인 → 데이터 탐색 → 제안·미리보기 → 사용자 승인 → 실행 → 결과 검토·출력 승인 → Excel 반영**.
- 수정·재제안 횟수는 제한하지 않는다. 승인 취소 시 현재 실행 시도를 멈추고 의견을 받아 다시 제안한다. 작업 전체 삭제와 구별한다.
- 원본은 보존하고 확인한 결과를 출력한다. 승인된 규칙·결과와 실제 실행 대상을 일치시킨다.
- 표기 정리와 동일 항목으로 묶는 판단을 구별한다. 전압·치수·재질·제조사·앞자리 0 등 의미 있는 차이를 임의로 제거하지 않는다.
- 현재 우선순위는 **작은 데이터로 승인부터 Excel 출력까지 완성**하는 것. 대량 처리 최적화는 이후 진행한다.

## 2. 필수 기능과 진행 상태

`[x]`는 코드 구현을 뜻하며 내부망 실사용 검증 완료를 뜻하지 않는다.

### 구현됨

- [x] **Add-in과 서버 연결**: HTML·CSS·JS 작업창을 FastAPI에서 제공. 카드 요청과 직접 채팅 입력을 지원한다.
- [x] **모델·환경 선택**: 단일 `src/config.yml`의 환경 설정을 `KCS_ENV`로 선택. Codex CLI와 HTTP 모델 연결 코드가 있다.
- [x] **대화와 작업 연결**: 메모리 대화 기록, 요청 ID 재시도 처리, 활성 Job 연결, 후속 질문에 작업 문맥 전달.
- [x] **작업 생성·조회·분석 API**: `/jobs`에서 모델규격과 해외거래처 작업을 구분해 각 서비스로 연결한다.
- [x] **모델규격 표본 확인**: 거래품명·신고품명·모델규격의 세 열을 매핑하고 앞부분 최대 20행을 읽어 LLM이 분석한다.
- [x] **모델규격 미리보기**: 현재 고정된 두 규칙(`trim`, `collapse_whitespace`)을 Python으로 적용. 원본·결과와 변경 행을 보여준다. LLM이 실행 규칙을 만드는 단계는 아직 아니다.
- [x] **표본 승인·출력 코드**: Job에 미리보기와 승인 ID를 보관하고, 승인한 표본 전체를 새 시트에 출력한다. 변경 행 강조와 동일 결과의 중복 시트 생성 방지가 있다.
- [x] **해외거래처 후보 검토**: 부호·국가코드·상호명 세 열을 읽고 같은 국가 내 상호 정규화·유사도 후보를 생성한다. LLM이 후보를 검토하며 자동 부호 통합·신규 부호 발급은 하지 않는다.
- [x] **회귀 테스트**: 이번 확인에서 Python 9개·JS 11개 통과. 가짜 모델·Office 응답 기반이며 실제 Excel 쓰기와 내부망 호환성 검증을 대체하지 않는다.

### 앞으로 필요한 기능

- [ ] **표본 출력 실기 확인**: 승인한 결과와 새 시트의 값 일치, 텍스트·앞자리 0 보존, 중복 클릭, 부분 실패 후 재시도를 실제 Excel에서 확인한다.
- [ ] **추가 데이터 확인·대조**: ERP 자료·정답 예시·참조표를 작업에 연결하고 원본과의 대응 관계, 일치·불일치·누락을 확인한다. 직접 ERP 접속은 필수로 가정하지 않으며 우선 사용자가 가져온 자료를 대상으로 한다.
- [ ] **숙련자 전처리 지식 저장·추천**: 사전 등록한 처리 방식의 적용 조건·예외·예시를 현재 데이터와 비교해 추천하고, 어떤 방식을 근거로 제안했는지 보여준다.
- [ ] **대화로 실행 규칙 제안**: LLM의 설명을 검증 가능한 규칙 데이터로 연결하고, 지원하지 않는 규칙은 실행하지 않는다.
- [ ] **수정·재승인 반복**: 사용자 의견에 따라 새 규칙 버전과 미리보기를 만든다. 이전 승인은 무효화하고 새 결과에 다시 승인받는다. 취소·작업 전환도 명시적으로 처리한다.
- [ ] **작업별 완료 경로**: 모델규격은 그룹화 근거와 원본 대응 결과까지, 거래처는 후보의 사용자 확정과 결과 출력까지 연결한다.
- [ ] **수식 작업**: 대상 범위·의도 확인 → 수식 제안·미리보기 → 승인 → 지정 위치 입력을 구현한다. 현재는 요청 분류·안내 수준이다.
- [ ] **화면과 작업 상태 복원**: Add-in을 다시 열 때 서버에 남아 있는 Job·미리보기·승인 상태로 카드를 복원한다. 출력 승인과 실제 쓰기 완료를 구별한다.
- [ ] **전체 데이터 실행**: 모델규격 최대 40만 행을 목표로 분할 수집·전송·처리·출력을 구현한다. 데이터 출처·원본 행 번호를 보존하고 진행·실패·취소 상태를 제공한다.
- [ ] **내부망 검증**: Linux/Python 3.12 서버, 내부 LLM, Windows Excel 2108·Excel 2021 대상에서 등록·통신·Office.js 리소스·인증서·실행 환경과 실제 쓰기를 확인한다.

## 3. 있으면 좋은 기능

- 자주 쓰는 요청 템플릿, 개인 즐겨찾기.
- 결과 비교·필터·검토용 파일 다운로드, 출력 시트·열 선택.
- 같은 데이터셋을 정제·통계 등 여러 Job에서 재사용.
- 장시간 작업의 재개·이력 보존. 영구 저장은 필요성과 저장 정책이 정해진 뒤 선택한다.
- 새로운 통계·분석 작업은 구체적인 요구가 생겼을 때 추가한다.

## 4. 현재 구조와 FastAPI 설계 원칙

```text
src/
  main.py, runtime.py           # 앱 수명주기·설정에 따른 실행 객체 구성
  api/                          # chat.py, jobs.py: HTTP 처리 / router.py: 연결
  services/chat.py              # 대화 처리·문맥·작업 진행 연결
  services/chat_state.py        # 대화가 기다리는 입력과 활성 Job
  services/jobs/
    model_normalization/        # 작업 데이터·정제·미리보기·승인
    counterparty_cleanup/       # 거래처 후보 생성·LLM 검토
  agents/                       # 공통 LLM 노드·요청 분류
  graphs/                       # 노드 실행 순서와 실행 중 상태
  models/                       # 모델 연결 어댑터
  prompts/                      # 시스템 프롬프트 YAML
  repositories/                 # 대화 저장소
  static/excel/                 # api_client, excel_bridge, taskpane,
                                # 작업별 controller와 UI
```

현재 코드는 **하나의 FastAPI 앱 안에서 기능별로 업무를 나누는 실용적인 계층형 구조**다. 아래 내용은 소스에서 확인한 현재 구현을 설명한다.

- **앱 시작 시 조립**: `main.py`의 lifespan에서 runtime·대화 저장소·Job 딕셔너리를 준비하고 `app.state`에 둔다. `runtime.py`가 환경 설정에 맞춰 모델과 프롬프트별 그래프를 구성한다. Add-in 정적 파일도 같은 앱에서 제공한다.
- **HTTP와 업무 처리 분리**: 기능별 API가 Pydantic으로 입력을 검증하고 필요한 서비스에 전달한다. `api/router.py`는 라우터를 연결한다. 단순 대화 생성·조회는 API에서 저장소를 직접 호출한다.
- **함수 중심의 의존성 전달**: API가 `app.state`에서 꺼낸 runtime·저장소·jobs를 서비스 함수의 인자로 넘긴다. 서비스는 FastAPI의 `Request`를 참조하지 않으며, `Depends`나 별도 DI 컨테이너를 중심으로 한 구조는 아니다.
- **작업별 구현 분리**: `services/jobs` 아래에 모델규격과 해외거래처의 스키마·처리 함수·규칙 엔진을 둔다. 현재 `/jobs`는 요청 모델 또는 저장된 Job 타입으로 서비스를 선택한다.
- **대화·업무 상태와 그래프 실행 구분**: `services/chat.py`는 대화 기록과 활성 작업으로 문맥을 만들고 진행을 연결한다. `graphs`는 한 번의 모델·도구 실행 순서와 실행 중 메시지를 관리한다. 승인 대기를 그래프 실행 상태로 영구 저장하는 구조는 아니다.
- **모델 판단과 계산 분리**: 모델 연결은 `models`, 시스템 프롬프트는 `prompts`에 둔다. LLM은 분석·후보 검토를 수행하고, 규칙 엔진은 Python으로 계산한다. 현재 분석 요청은 비동기 함수 안에서 결과를 기다리며 별도 작업 큐를 사용하지 않는다.
- **오류·저장 경계**: 서비스 오류를 API에서 HTTP 상태와 메시지로 변환한다. 대화 저장은 repository, Job 저장은 공유 딕셔너리가 담당한다. 현재 메모리 실행에서는 재시작 시 상태가 사라지며 PostgreSQL 구현은 업무 상태·화면 액션까지 동등하게 지원하지 않는다.
- **Excel 클라이언트 분리**: `api_client`는 HTTP, `excel_bridge`는 Excel 읽기·쓰기, 작업별 controller는 진행 흐름, UI 파일은 표시를 담당한다. `taskpane`이 대화 세션과 필요한 함수를 연결한다.

## 5. 다음 작업과 인수인계

1. 모델규격 표본의 승인·Excel 출력 실기 확인을 끝낸다.
2. 추가 자료 대조와 숙련자 전처리 지식의 등록·추천을 규칙 제안에 연결한다.
3. LLM 규칙 제안 → 수정 의견 → 새 미리보기 → 재승인의 연결을 완성한다.
4. 거래처 검토·수식 작업도 필요한 승인·출력 경로를 연결한다.
5. 그다음 전체 데이터 처리와 내부망 검증을 진행한다.

기본 협업 방식은 사용자가 직접 코드를 반영하고 assistant가 위치·이유·검증 방법을 안내하는 것이다. 직접 수정을 요청한 범위만 대신 수정한다. 불필요한 계층·의존성은 추가하지 않고 프롬프트는 `src/prompts`에 둔다. 다른 대화에서는 이 문서를 시작점으로 읽되 실제 파일을 먼저 확인한다.
